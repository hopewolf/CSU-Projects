/*! \file main.cpp: main function for histogram */
#include <histogram.h>
#include <iostream>
using std::cout;
using std::endl;
using std::cin;
using std::cerr;
#include <fstream>
using std::ifstream;


int main(int argc, char* argv[])
{
    if(argc != 2) {
        cerr << "Missing file as argument" << endl;
        return -1;
    }
    histogram temp(arvg[1]);
    return 
    
    
}
