#include <Stem.h>
#include <gtest/gtest.h>
/*
TEST(portAlg, step1) // to disable type DISABLED_...
{
	Stem ss;
    	ASSERT_EQ("",ss.step1(""));
	EXPECT_EQ("abc", ss.step1("abc"));
	EXPECT_EQ("def", ss.step1("\'def"));
}

TEST(portAlg, step2)
{
	Stem ss;
	string t = "abcsses";
	ss.step2(t);
	EXPECT_EQ("abcss", t);
}


TEST(portAlg, step3) 
{
    	Stem ss;
    	ASSERT_TRUE(ss.isVowel('o'));
    	string test = "conditioning";
    	ss.step3(test);
   	EXPECT_EQ("condition", test);
    	EXPECT_NE("conditioning", test);
}
*/

